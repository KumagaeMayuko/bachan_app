<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @diary.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @diary.content %>
</p>

<p>
  <strong>Picture:</strong>
  <%= link_to @diary.picture.filename, @diary.picture if @diary.picture.attached? %>
  <%= image_tag @diary.picture %>
</p>

<%= link_to 'Edit', edit_diary_path(@diary) %> |
<%= link_to 'Back', diaries_path %>
